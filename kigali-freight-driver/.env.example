EXPO_PUBLIC_API_BASE_URL=http://192.168.1.71:5000

# Optional: enables real crash/error reporting (lib/crashReporting.ts).
# Leave unset to keep it a no-op — get a DSN from sentry.io once a project
# is created there; nothing else in the app changes either way.
EXPO_PUBLIC_SENTRY_DSN=